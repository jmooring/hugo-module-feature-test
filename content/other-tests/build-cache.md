---
title: Build cache
---

This page tests Hugo's build cache for image processing. On the first build, the images are processed and the results are cached, which takes several seconds. On subsequent builds, Hugo uses the cached results, completing in under a second. The elapsed time shown below indicates whether the cache was used.
